.\" pyCLIG manual page
.\" Based on clig by Harald Kirsch (C) 1995-2004 (clig@geggus.net)
.\" pyCLIG (C) 2026 Scott Ransom and Claude (Anthropic)
.\"
.\" This file was generated by pyclig -- command line interface generator
.\"
.\" cligPart markers delimit auto-generated sections.
.\" Remove a pair to hand-edit that section instead.
.\"
.\" cligPart TITLE
.TH "toas2dat" 1 "03Jul26" "pyCLIG-manuals" "Programmer's Manual"
.\" cligPart TITLE end

.\" cligPart NAME
.SH NAME
toas2dat \- Converts TOAs into a binned time series.
.\" cligPart NAME end

.\" cligPart SYNOPSIS
.SH SYNOPSIS
.B toas2dat
-n numout
-dt dt
[-t0 t0]
-o outfile
[-text]
[-float]
[-sec]
[--] file
.\" cligPart SYNOPSIS end

.\" cligPart OPTIONS
.SH OPTIONS
.IP -n
The number of bins in the output time series,
.br
1 Long value between 0 and oo.
.IP -dt
Time interval in seconds for output time bins,
.br
1 Double value between 0 and oo.
.IP -t0
Time for the start of bin 0 (same units as the TOAs),
.br
1 Double value.
.IP -o
Name of the output time series file,
.br
1 String value
.IP -text
TOAs are ASCII text (default is binary double).
.IP -float
TOAs are binary floats (default is binary double).
.IP -sec
TOA unit is seconds (default is days).
.IP file
Input TOA file name.
.\" cligPart OPTIONS end

.\" cligPart DESCRIPTION
.SH DESCRIPTION
toas2dat bins a list of event/photon arrival times (TOAs)
into a regularly-sampled time series
.RI ( .dat
file, plus matching
.IR .inf ),
so that data that only exists as individual arrival times (e.g.
gamma-ray or X-ray photon events) can be searched for periodicities
with
.BR realfft (1)
and
.BR accelsearch (1)
just like any other PRESTO time series.
.B -n
and
.B -dt
set the number of output bins and the bin width (seconds);
.B -t0
sets the time of bin 0 (defaults to the first TOA).  Input TOAs are
binary doubles by default, in days; use
.B -text
for ASCII input,
.B -float
for binary single-precision input, and
.B -sec
if the TOA unit is seconds rather than days.

.\" cligPart DESCRIPTION end

.\" cligPart SEE ALSO
.SH SEE ALSO
realfft(1), accelsearch(1), prepdata(1)
.\" cligPart SEE ALSO end

.\" cligPart AUTHOR
.SH AUTHOR
Scott Ransom <sransom@nrao.edu>
.\" cligPart AUTHOR end
