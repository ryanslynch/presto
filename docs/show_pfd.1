.\" pyCLIG manual page
.\" Based on clig by Harald Kirsch (C) 1995-2004 (clig@geggus.net)
.\" pyCLIG (C) 2026 Scott Ransom and Claude (Anthropic)
.\"
.\" This file was generated by pyclig -- command line interface generator
.\"
.\" cligPart markers delimit auto-generated sections.
.\" Remove a pair to hand-edit that section instead.
.\"
.\" cligPart TITLE
.TH "show_pfd" 1 "03Jul26" "pyCLIG-manuals" "Programmer's Manual"
.\" cligPart TITLE end

.\" cligPart NAME
.SH NAME
show_pfd \- Displays or regenerates the Postscript for a 'pfd' file created by prepfold.
.\" cligPart NAME end

.\" cligPart SYNOPSIS
.SH SYNOPSIS
.B show_pfd
[-noxwin]
[-showfold]
[-scaleparts]
[-allgrey]
[-justprofs]
[-portrait]
[-events]
[-infoonly]
[-fixchi]
[-samples]
[-normalize]
[-killsubs killsubsstr]
[-killparts killpartsstr]
[--] infile ...
.\" cligPart SYNOPSIS end

.\" cligPart OPTIONS
.SH OPTIONS
.IP -noxwin
Do not show the result plots on-screen, only make postscript files.
.IP -showfold
Use the input fold paramters (i.e. not the optimized values) when showing the plot.
.IP -scaleparts
Scale the part profiles independently.
.IP -allgrey
Make all the images greyscale instead of color.
.IP -justprofs
Only output the profile portions of the plot.
.IP -portrait
Orient the output in portrait mode (for -justprofs).
.IP -events
The folded data were events instead of samples or bins.
.IP -infoonly
Display the pfd info and exit without generating plots..
.IP -fixchi
Adjust the reduced chi^2 values so that off-pulse reduced chi^2 = 1.
.IP -samples
Treat the data as samples and not as finite-duration integrated data.
.IP -normalize
Normalize stats for each fold (i.e. to bandpass flatten subbands).
.IP -killsubs
Comma separated string (no spaces!) of subbands to explicitly remove from analysis (i.e. zero out).  Ranges are specified by min:max[:step],
.br
1 String value
.IP -killparts
Comma separated string (no spaces!) of intervals to explicitly remove from analysis (i.e. zero-out).  Ranges are specified by min:max[:step],
.br
1 String value
.IP infile
The input 'pfd' file name..
.\" cligPart OPTIONS end

.\" cligPart DESCRIPTION
.SH DESCRIPTION
show_pfd re-displays or regenerates the postscript summary
plot(s) for a
.BR prepfold (1)
.I .pfd
file, without re-folding the raw data.  Run with no other options it
just reproduces the original plot; the other options let you inspect
or post-process a fold after the fact.
.B -killsubs
and
.B -killparts
zero out specific frequency subbands or time intervals (e.g. ones
found to contain interference after the original fold) and
regenerate the plot and statistics with them excluded, which is much
cheaper than re-running
.BR prepfold (1)
from raw data.
.B -fixchi
adjusts the reported reduced chi^2 values so that the off-pulse
region has reduced chi^2 = 1, partially compensating for chi^2
inflation caused by interference.
.B -justprofs
outputs only the phase-vs-time image and the integrated pulse profile
panel, useful for publication-quality figures;
.B -infoonly
prints the
.I .pfd
file's header information and exits without plotting at all.
.PP
Example: replot a fold after zeroing out a bad subband and interval,
and fixing the chi^2 normalization:
.RS
.nf
show_pfd -killsubs 10:12 -killparts 3 -fixchi mypulsar_DM44.30.pfd
.fi
.RE
.PP
Example: make a clean, publication-quality phase-vs-time and
integrated-profile plot, bandpass-flattening the subbands first:
.RS
.nf
show_pfd -justprofs -normalize mypulsar_DM44.30.pfd
.fi
.RE

.\" cligPart DESCRIPTION end

.\" cligPart SEE ALSO
.SH SEE ALSO
prepfold(1), prepfold_multi(1)
.\" cligPart SEE ALSO end

.\" cligPart AUTHOR
.SH AUTHOR
Scott Ransom <sransom@nrao.edu>
.\" cligPart AUTHOR end
